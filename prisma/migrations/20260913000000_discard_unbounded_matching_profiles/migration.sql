-- Derived profiles may contain messages outside a participant's consent window.
-- Rebuild them from the corrected consent boundaries on the next matching request.
DELETE FROM "MatchingTopicProfile";
