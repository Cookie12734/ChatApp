# AGENTS.md

## このプロジェクトは何か

`connect` は、気軽に会話や相談をしたい人のための会員登録制チャットアプリです。
フレンドとのDM、話題別のランダムマッチング、サーバー・グループでの会話を提供します。
Next.js・React・TypeScript、tRPC、Auth.js、Prisma・PostgreSQL、SSEで構成されています。

## 最初に読むもの

- 目的・機能・構成：`README.md`
- 作業開始時の検証選択・Skill適用ルール：[エージェント作業手順](docs/agent-workflow.md)
- 現行UIの設計方針：`design.md`、`tokens.css`、`src/styles/globals.css`
- 実行コマンド・依存関係：`package.json`、`package-lock.json`
- 環境変数・データ構造：`.env.example`、`src/env.js`、`prisma/schema.prisma`
- 検証方法：`playwright.config.ts`、`e2e/`、`src/`内の`*.test.mjs`
- 過去の変更：関連ファイルのGit履歴と回帰テスト

この索引には実在する資料を載せてください。新しい資料を作る場合は対応する参照先を更新し、同じルールを複数箇所に複製しないでください。
`.hallmark/log.json`は履歴、`.hallmark/preflight.json`は補助キャッシュです。現行仕様は`design.md`で確認し、古い記録を現在の指示として適用しないでください。

## 必ず守ること

コマンドはプロジェクトルートで実行してください。

- 検証：[作業種別ごとの必要検証](docs/agent-workflow.md#検証の選び方)を選び、下表のコマンドで実行してください。読み取り監査・文書のみの変更にアプリのE2Eは要求しません。
- E2E実行前に、DBとアプリがテスト用であり、データの作成・削除が許可された対象であることを確認してください。現状のE2Eは通常の環境設定を読み込み、DBへ書き込みます。接続先が不明なまま実行しないでください。
- `npm run dev`は`predev`で`prisma migrate deploy`も実行します。`npm run db:generate`の実体は`prisma migrate dev`です。名称だけで副作用を判断しないでください。
- 依頼で明示された場合を除き、`.env`、秘密を含む`.env.*`、`public/uploads/`内のユーザーデータ、プロジェクト外のファイルを変更・削除しないでください。`.env.example`には秘密を記載しないでください。
- `.git/`内部、`node_modules/`、`.next/`、`generated/`、`next-env.d.ts`は手作業で編集しないでください。必要な変更は正規のGit操作・インストール・生成コマンドで行ってください。
- 既存の`prisma/migrations/`内のマイグレーションは書き換えず、必要な変更は新規マイグレーションにしてください。
- ユーザーの未コミット変更を上書き・取り消ししないでください。秘密情報や認証情報をログ、回答、コミットへ出さないでください。
- DB更新、外部サービス接続・送信、デプロイ、権限拡張は、その操作が既に承認されている範囲を確認してください。未承認なら実行前に確認し、承認済みの同じ操作について重ねて確認しないでください。
- UI変更では`design.md`を守り、認証・権限制御・ブロック・メッセージの重複防止など、変更箇所に関係する既存動作を確認してください。
- コミット自体は依頼された場合に行ってください。コミット前にも作業種別に必要な検証の成功を確認し、検証後に関連ファイルを変更した場合は影響する検証を再実行してください。

| 検証                    | コマンド                                                                                |
| ----------------------- | --------------------------------------------------------------------------------------- |
| 単体テスト全件          | `npm test`                                                                              |
| 型チェック              | `node node_modules/typescript/bin/tsc --noEmit --incremental false`                     |
| アプリ・E2Eコードのlint | `node node_modules/eslint/bin/eslint.js src e2e --no-cache`                             |
| 形式チェック            | `node node_modules/prettier/bin/prettier.cjs --check`に変更した対応ファイルのパスを渡す |
| E2E全件                 | `npm run test:e2e`（安全なテスト環境の確認後）                                          |

既存の違反を見つけても、無関係なファイルを一括修正して隠さないでください。今回の失敗と既存の失敗を根拠付きで区別し、必要な検証が失敗したままなら未解決として報告してください。

## 完了の定義

以下をすべて満たしたときだけ「完了」と報告してください。

- 依頼された範囲を満たし、作業種別と影響範囲に必要な検証が成功している。
- 差分と関連する処理を読み、想定外の副作用がないか自分で確認している。
- UI・操作の変更は、テスト用環境のブラウザで実際に操作して確認している。
- 変更の意図、実行した検証と結果を簡潔に報告し、未実行・失敗・未解決事項を成功した扱いにしていない。

動作の不具合修正では再発を検知できるテストを確保してください。既存テストで不足する場合は追加・更新します。指示・資料の修正では参照先と矛盾の解消を確認し、文章を固定するだけのテストは作らないでください。
必要な検証が実行できない場合は、理由と残作業を報告し、「完了」とは報告しないでください。作業種別上不要な検証は完了を妨げません。

## 判断に迷ったとき

既存の実装・資料・承認範囲から決められる可逆的な実装判断は進め、結果に影響する仮定を報告してください。
仕様、データ保護、外部への作用、変更範囲について結果を左右する不明点が残る場合は、実行可能な選択肢と影響・推奨理由を示して確認してください。
既に回答・承認された内容をSkillの定型質問で聞き直さないでください。不明な事実を確認済みとして扱わないでください。
回答が必要な作業は待ち、回答に依存しない調査は続けてください。

## 指示の優先順位

1. 私（ユーザー）のその場の指示
2. このAGENTS.mdと、ここから委任する`docs/agent-workflow.md`の作業ルール・`design.md`のUI設計方針
3. Skillsに書かれた手順

Skillを使う場合は、先に作業手順の該当節で適用範囲を確認してください。Hallmarkと`designer-skill`の役割分担も同手順に定めます。
上位と矛盾する下位の指示は適用せず、該当したSkill名と理由を簡潔に報告してください。Skillは依頼範囲、承認範囲、モデル設定を自動的に広げるものではありません。
これはプロジェクト内の指示の優先順位です。実行環境のシステム・開発者指示やセキュリティ制約を上書きするものではありません。
